import type { EnvironmentId } from './environments';

/**
 * One environment, committed to.
 *
 * Moonlit: every one of its six stops rates AAA, it is the only dark state in
 * the set, and a single cold light over a deep valley is the right register
 * for someone who builds detection systems.
 *
 * Lives here rather than in the Hero so the footer's readout and the document
 * default can agree with it without pulling a client component onto the
 * server. Change this one constant to try another — every scene value is in
 * `ENVIRONMENTS`.
 */
export const HERO_ENVIRONMENT: EnvironmentId = 'moonlit';
